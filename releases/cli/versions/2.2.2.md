## 🏥 Context leak fix in browser command

`context.WithTimeout` in the per-URL tab loop returned a cancel function that was discarded (`_`). The timeout context leaked on every iteration until the parent context was cancelled or the timer fired. The cancel function is now captured and deferred.
